#!/usr/bin/python3

def max_integer(my_list=[]):
    if len(my_list) == 0:
        return None
    else:
        x = my_list[0]
        for i in range (len(my_list)):
            if (x < my_list[i])
                x = my_list[i]
              
    return x 
